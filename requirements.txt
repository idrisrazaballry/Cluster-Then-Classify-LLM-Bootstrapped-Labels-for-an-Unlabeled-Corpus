pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7

# Phase 1 -- embeddings
sentence-transformers>=2.2

# Phase 2 -- projection and density clustering
umap-learn>=0.5          # optional; PCA fallback if absent
hdbscan>=0.8             # optional; sklearn>=1.3 has HDBSCAN built in

# Phase 3 -- LLM labelling
langchain-google-genai>=2.0

# notebooks
jupyter>=1.0
